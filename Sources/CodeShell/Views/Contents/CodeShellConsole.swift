import SwiftUI

public struct CodeShellConsole: View {
  public init() {}

  public var body: some View {
    Text("Console").frame(minWidth: 0, maxWidth: .infinity, minHeight: 0, maxHeight: .infinity)
  }
}
