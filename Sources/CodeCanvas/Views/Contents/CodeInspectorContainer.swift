#if canImport(SwiftUI)
  import SwiftUI

  public struct CodeInspectorContainer: View {
    public init() {}

    public var body: some View {
        VStack {
            Spacer()
            Text("Inspector")
            Spacer()
        }
    }
  }
#endif
